{#
/**
 * @file
 *
 * Provides the TREZOR connect button markup.

 * Available variables:
 * - attributes: HTML attributes for the containing element.
 * - children: The rendered child elements of the container.
 *
 * @ingroup themeable
 */
#}
<{{ tag }}{{ attributes }}>
  {% if tag != "trezor:login" %}
  {{ text }}
  {% endif %}
  {{ children }}
</{{ tag }}>
